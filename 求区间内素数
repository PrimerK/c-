#include<stdio.h>
#include<math.h>
int checkprime(int num)
{
    if (num==1)return 0;
    if (num==2)return 1;
    for(int i=2;i<=sqrt(num);i++)
    {
        if(num%i==0)return 0;
    }
    return 1;
}

int main()
{
    int m,n;
    int cnt=0;
    scanf("%d %d",&m,&n);
    if(m>n)return 1;
    for(int i=m;i<=n;i++)
    {
        if(checkprime(i)){
            printf("%d ",i);
            cnt+=1;
        }
    }
    printf("\n");
    printf("count = %d",cnt);
    return 0;
}
