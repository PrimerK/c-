#include<stdio.h>

int main()
{
    char str[100];
    fgets(str,sizeof(str),stdin);
    char*i=str;
    for(;*i!='\0';i++){
        if(*i>='0'&&*i<='9'){

        }
        else{
            printf("%c",*i);
        }
    }
    return 0;
}
